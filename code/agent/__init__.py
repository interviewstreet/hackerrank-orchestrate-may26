"""
agent/ — Core triage agent package.

Modules:
  classifier  — Pre-LLM ticket classification and routing
  safety      — Safety checks and escalation heuristics
  retriever   — Knowledge base retrieval (BM25 + semantic)
  responder   — LLM-based response generation with structured output
"""
