"""
corpus/ — Corpus loading and indexing package.

Modules:
  loader   — Reads and parses all .md files from data/ into document dicts
  scraper  — (Optional) Utility to re-scrape/refresh corpus from source URLs
"""
